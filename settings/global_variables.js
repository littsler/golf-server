const default_max_photo_width = 400;
const database_expiration_days = 30;
const default_photo_count_per_page = 10;
const num_of_top_checkins = 3;
const photo_ref_segments_separator = '----';

module.exports = {
    default_max_photo_width, default_photo_count_per_page, database_expiration_days, num_of_top_checkins, photo_ref_segments_separator,
};